# PlayNest

PlayNest is a prototype app for expat parents to find child-friendly spaces, playdates, group activities, trusted childcare, lessons, and parent community support for families under pre-schooler age.

## Development

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

The production build is generated in `dist/`.
```
